name: Clojure CI Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    env: # Environment variable for the entire job
      CI_ENV: true

    steps:
      - uses: actions/checkout@v6
      - name: Set up Java 21
        uses: actions/setup-java@v5
        with:
          java-version: 21
          distribution: 'temurin'
      - uses: DeLaGuardo/setup-clojure@master
        with:
          cli: latest
      - name: Run tests
        env:
          MANAGEMENT_CLIENT_ID: ${{ secrets.MANAGEMENT_CLIENT_ID }}
          MANAGEMENT_CLIENT_SECRET: ${{ secrets.MANAGEMENT_CLIENT_SECRET }}
          TEST_CLIENT_ID: ${{ secrets.TEST_CLIENT_ID }}
        run: clj -A:test
      - name: Cleanup test users
        env:
          MANAGEMENT_CLIENT_ID: ${{ secrets.MANAGEMENT_CLIENT_ID }}
          MANAGEMENT_CLIENT_SECRET: ${{ secrets.MANAGEMENT_CLIENT_SECRET }}
          TEST_CLIENT_ID: ${{ secrets.TEST_CLIENT_ID }}
        run: lein with-profile +dev run -m pigeon-scoops-backend.cleanup
